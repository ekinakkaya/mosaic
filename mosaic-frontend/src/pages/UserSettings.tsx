import React from 'react';

const UserSettings: React.FC = () => {
  return (
    <div className="flex justify-center items-center h-screen">
      <h1 className="text-2xl font-bold">User Settings Page</h1>
    </div>
  );
};

export default UserSettings;
